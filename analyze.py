import mysql.connector

mydb = mysql.connector.connect(
  host="localhost",
  user="root",
  passwd="!@)59380",
  database="footballStats"
)

def pick_best_team(excluded_player_ids):


def get_sorted_players(sort_by = "pos")
